import React from 'react';

class SearchList extends React.Component{
    render(){
        return(
            <div style={{color:'white'}}>Search List!</div>
        )
    }
}

export default SearchList;