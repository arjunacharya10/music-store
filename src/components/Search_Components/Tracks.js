import React from 'react';
import Track from './Track';

const Tracks = ({trackList})=>{
    return(
        <div className="ui" style={{color:'white'}}>
            <Track/>
        </div>
    );
}

export default Tracks;